const deletePlant = (id) => {
    // Logic to send a DELETE request to the server and handle the response
  };
  
  // Example usage as an event handler
  <button onClick={() => deletePlant(1)}>Delete Plant</button>